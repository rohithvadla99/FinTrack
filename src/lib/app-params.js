// app-params.js — retained for compatibility, no longer used for base44 cloud
export const appParams = {
  appId: 'local',
  token: null,
  functionsVersion: null,
  appBaseUrl: '/',
  fromUrl: typeof window !== 'undefined' ? window.location.href : '/',
};
